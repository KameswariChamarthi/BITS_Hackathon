import 'package:flutter/material.dart';
import '../screens/disaster_report_screen.dart';

class DisasterDialog extends StatelessWidget {
  const DisasterDialog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const DisasterReportScreen();
  }
}
